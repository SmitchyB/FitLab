<Page x:Class="FitLab.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:FitLab.Components"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage"
      Background="#222">

    <Grid>
        <!-- Main Content Layer -->
        <ScrollViewer>
            <StackPanel Margin="20">

                <!-- Welcome & Week Info Section -->
                <Border BorderBrush="#9a42ff" BorderThickness="2" CornerRadius="5" Background="#222" Padding="15" Margin="0,0,0,20">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock x:Name="WelcomeText" FontSize="24" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="WeekText" FontSize="18" Margin="0,5,0,0" Foreground="White"/>
                        <TextBlock x:Name="DayText" FontSize="18" Margin="0,2,0,0" Foreground="White"/>
                    </StackPanel>
                </Border>

                <!-- Quick Actions Section -->
                <TextBlock Text="Quick Actions" FontSize="22" FontWeight="Bold" Margin="0,0,0,10" Foreground="White"/>

                <!-- Daily Workout Plan -->
                <Border BorderBrush="#9a42ff" BorderThickness="2" CornerRadius="8" Padding="10" Margin="0,0,0,20">
                    <StackPanel x:Name="ExerciseQuickList" Margin="0,10,0,0">
                        <TextBlock Text="Today's Exercises" FontSize="18" FontWeight="Bold" Foreground="#9a42ff" Margin="0,0,0,10"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Modal Overlay -->
        <Grid x:Name="ModalOverlay" Visibility="Collapsed"
              Background="#80000000" Panel.ZIndex="100">
              <ContentControl x:Name="ExerciseCompletionModal"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Visibility="Collapsed"/>

        </Grid>
    </Grid>
</Page>

