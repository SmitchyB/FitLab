<Page x:Class="FitLab.Pages.MyBodyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="1000"
      Title="MyBodyPage">

    <ScrollViewer>
        <StackPanel Margin="20" Orientation="Vertical" VerticalAlignment="Top">

            <!-- Basic Info -->
            <GroupBox Header="Basic Information" Margin="0,0,0,10"
                      BorderBrush="#9a42ff" Foreground="#9a42ff">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center" Margin="5"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtName" Margin="5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Height:" VerticalAlignment="Center" Margin="5"/>
                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="5">
                        <TextBox x:Name="TxtHeight" Width="60" Margin="0,0,5,0"/>
                        <ComboBox x:Name="CmbHeightUnit" Width="80">
                            <ComboBoxItem Content="cm"/>
                            <ComboBoxItem Content="feet/in"/>
                        </ComboBox>
                    </StackPanel>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Weight:" VerticalAlignment="Center" Margin="5"/>
                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="5">
                        <TextBox x:Name="TxtWeight" Width="60" Margin="0,0,5,0"/>
                        <ComboBox x:Name="CmbWeightUnit" Width="80">
                            <ComboBoxItem Content="kg"/>
                            <ComboBoxItem Content="lbs"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <!-- Goals -->
            <GroupBox Header="Goals" BorderBrush="#9a42ff" Foreground="#9a42ff" Margin="0,0,0,10">
                <StackPanel>
                    <ItemsControl x:Name="GoalsPanel">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Gray" BorderThickness="1" Padding="5" Margin="5">
                                    <StackPanel Orientation="Horizontal">
                                        <StackPanel Width="300">
                                            <TextBlock Text="{Binding MealTime}" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Description}"/>
                                            <TextBlock Text="{Binding Calories, StringFormat={}{0} Calories}"/>
                                        </StackPanel>
                                        <Button Content="Edit" Tag="{Binding}" Click="BtnEditMeal_Click" Margin="2"/>
                                        <Button Content="Delete" Tag="{Binding}" Click="BtnDeleteMeal_Click" Margin="2"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Add Goal -->
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBox x:Name="TxtGoalTitle" Width="120" Margin="2"/>
                        <TextBox x:Name="TxtGoalDescription" Width="200" Margin="2"/>
                        <Button Content="Add Goal" Click="BtnAddGoal_Click" Margin="2"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- Date Picker -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Select Date for Food &amp; Water:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <DatePicker x:Name="DatePickerSelectedDate"
                            SelectedDateChanged="DatePickerSelectedDate_SelectedDateChanged"/>
            </StackPanel>

            <!-- Water Intake -->
            <GroupBox Header="Water Intake"
                      BorderBrush="#9a42ff"
                      Foreground="#9a42ff"
                      Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="BtnMinusWater"
                            Content="-"
                            Width="30"
                            Height="30"
                            Margin="5"
                            Click="BtnMinusWater_Click"/>
                    <TextBlock x:Name="TxtWaterCups"
                               FontSize="16"
                               VerticalAlignment="Center"
                               Margin="10"
                               Text="0 cups"/>
                    <Button x:Name="BtnPlusWater"
                            Content="+"
                            Width="30"
                            Height="30"
                            Margin="5"
                            Click="BtnPlusWater_Click"/>
                </StackPanel>
            </GroupBox>

            <!-- Food Intake -->
            <GroupBox Header="Food Intake"
                      BorderBrush="#9a42ff"
                      Foreground="#9a42ff">
                <StackPanel>
                    <ItemsControl x:Name="FoodIntakePanel">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Gray" BorderThickness="1" Padding="5" Margin="5">
                                    <StackPanel Orientation="Horizontal">
                                        <StackPanel Width="300">
                                            <TextBlock Text="{Binding MealTime}" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Description}"/>
                                            <TextBlock Text="{Binding Calories, StringFormat='Calories: {0}'}"/>
                                        </StackPanel>
                                        <Button Content="Edit" Tag="{Binding}" Click="BtnEditMeal_Click" Margin="2"/>
                                        <Button Content="Delete" Tag="{Binding}" Click="BtnDeleteMeal_Click" Margin="2"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Add Meal -->
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBox x:Name="TxtMealTime" Width="80" Margin="2"/>
                        <TextBox x:Name="TxtDescription" Width="160" Margin="2"/>
                        <TextBox x:Name="TxtCalories" Width="60" Margin="2"/>
                        <Button Content="Add Meal" Click="BtnAddMeal_Click" Margin="2"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</Page>
