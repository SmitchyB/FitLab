<Page x:Class="FitLab.Pages.WorkoutPlanPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:components="clr-namespace:FitLab.Components"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WorkoutPlanPage"
      Background="Transparent">

    <Grid Background="Transparent">
        <Border BorderBrush="#9a42ff"
                BorderThickness="2"
                CornerRadius="5"
                Margin="20"
                Background="Transparent">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
                <StackPanel Orientation="Vertical" Background="Transparent">

                    <TextBlock Text="Weekly Workout Routine"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#9a42ff"
                               Margin="0,0,0,20"/>
                    <!-- Sunday -->
                    <Expander Header="Sunday" IsExpanded="True" Margin="0,0,0,10" Foreground="#9a42ff">
                        <StackPanel>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Warmup" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="SundayWarmupExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Main Workout" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="SundayMainExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Cooldown" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="SundayCooldownExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Expander>

                    <!-- Monday -->
                    <Expander Header="Monday" IsExpanded="True" Margin="0,0,0,10" Foreground="#9a42ff">
                        <StackPanel>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Warmup" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="MondayWarmupExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />

                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Main Workout" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="MondayMainExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />

                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Cooldown" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="MondayCooldownExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Expander>

                    <!-- Tuesday -->
                    <Expander Header="Tuesday" IsExpanded="True" Margin="0,0,0,10" Foreground="#9a42ff">
                        <StackPanel>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Warmup" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="TuesdayWarmupExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Main Workout" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="TuesdayMainExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Cooldown" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="TuesdayCooldownExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Expander>

                    <!-- Wednesday -->
                    <Expander Header="Wednesday" IsExpanded="True" Margin="0,0,0,10" Foreground="#9a42ff">
                        <StackPanel>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Warmup" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="WednesdayWarmupExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Main Workout" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="WednesdayMainExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Cooldown" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="WednesdayCooldownExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Expander>

                    <!-- Thursday -->
                    <Expander Header="Thursday" IsExpanded="True" Margin="0,0,0,10" Foreground="#9a42ff">
                        <StackPanel>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Warmup" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="ThursdayWarmupExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Main Workout" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="ThursdayMainExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Cooldown" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="ThursdayCooldownExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Expander>

                    <!-- Friday -->
                    <Expander Header="Friday" IsExpanded="True" Margin="0,0,0,10" Foreground="#9a42ff">
                        <StackPanel>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Warmup" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="FridayWarmupExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Main Workout" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="FridayMainExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Cooldown" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="FridayCooldownExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Expander>

                    <!-- Saturday -->
                    <Expander Header="Saturday" IsExpanded="True" Margin="0,0,0,10" Foreground="#9a42ff">
                        <StackPanel>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Warmup" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="SaturdayWarmupExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Main Workout" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="SaturdayMainExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                            <Border BorderThickness="1" BorderBrush="#9a42ff" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="Cooldown" FontWeight="Bold" FontSize="16" Foreground="#9a42ff" Margin="0,0,0,5"/>
                                    <ItemsControl Name="SaturdayCooldownExercises"/>
                                    <Button Content="+ Add Exercise" Margin="0,5,0,0" HorizontalAlignment="Left" Click="AddExercise_Click" />
                                    <components:ExerciseSelector Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Expander>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>



